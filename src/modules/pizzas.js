// Action type constants

// Action creators

// Reducer and its initialState

const initialState = {
  pizzaList: [],
};

const pizzas = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// Export statement

export { pizzas };
